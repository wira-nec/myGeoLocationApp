import { ResizeElementDirective } from './resize-element.directive';

describe('ResizeElementDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeElementDirective();
    expect(directive).toBeTruthy();
  });
});
