import { OverflowPaneDirective } from './overflow-pane.directive';

describe('OverflowPaneDirective', () => {
  it('should create an instance', () => {
    const directive = new OverflowPaneDirective();
    expect(directive).toBeTruthy();
  });
});
