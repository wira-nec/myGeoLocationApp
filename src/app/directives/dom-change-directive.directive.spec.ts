import { DomChangeDirectiveDirective } from './dom-change-directive.directive';

describe('DomChangeDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DomChangeDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
